var valor = parseInt(prompt("Informe um valor: "));
var raiz = parseInt(prompt("Informe um valor para a raiz: "));
var limite = 10;

var soma = valor 
var resultado = valor.toString()


for(var i = 0; i < limite; i++){
    soma += raiz
    resultado += `, ${soma}`;
}
alert(`PA = ${resultado}`);